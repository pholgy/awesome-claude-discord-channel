{
  "name": "awesome-claude-discord-channel",
  "version": "0.1.0",
  "description": "A Discord channel plugin for Claude Code with a hardened visible-reply delivery contract.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "verify": "bun test && bun scripts/verify.mjs",
    "test": "bun run verify",
    "setup:community": "bun scripts/setup-community.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "discord.js": "^14.14.0"
  }
}
